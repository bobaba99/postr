# Postr

Academic poster web app. Anonymous-first auth, autosave, custom presets, asset uploads, reusable libraries, shareable links, and AI-powered poster scan. Monorepo: `apps/web` (Vite + React SPA), `apps/api` (Express LLM proxy), `packages/shared` (typed data model), Supabase for auth/DB/storage.
