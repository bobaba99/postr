# Experiment harness — commit the SCRIPTS + paper INPUTS (text.md, gold.json)
# only. Never commit deps or generated run artifacts.
node_modules/
RESULTS.json
RESULTS.md
papers/*/findings.*.json
# Raw Elicit extraction material used to draft gold.json — data, not a committed fixture.
papers/*/source.json
